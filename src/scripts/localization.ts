// const langObj = {

// };

// const languages = document.querySelectorAll(".language_variant");

// languages.forEach(lang => {
//   lang.addEventListener("click", event => {
//     changeURL(lang as HTMLLIElement);
//   });
// });

// function changeURL(lang: HTMLLIElement) {
//   const mustLang = lang.innerText;
//   window.location.hash = window.location.hash + "#" + mustLang; 

// }

